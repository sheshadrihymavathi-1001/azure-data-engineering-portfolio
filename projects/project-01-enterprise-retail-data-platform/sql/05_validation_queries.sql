-- Project 01 validation queries.
-- Cover row counts, orphan detection, duplicate keys, amount reconciliation,
-- status-history consistency, inventory matrix completeness, and checkpoint state.

-- TODO: add the validated validation query set.
